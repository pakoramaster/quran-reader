import { TextInput } from 'react-native';

import type { FolioTextInputProps } from './FolioTextInput.types';

export function FolioTextInput(props: FolioTextInputProps) {
  return <TextInput {...props} />;
}
